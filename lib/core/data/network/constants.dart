class ApiConstants {
  static const Duration defaultTokenExpirationTime = Duration(minutes: 1);
  static const Duration defaultConnectTimeout = Duration(minutes: 1);
  static const Duration defaultReceiveTimeout = Duration(minutes: 1);
  static const Duration defaultSendTimeout = Duration(minutes: 1);
}
